# 📚 Методичка: Развертывание сайта "База лицензий на недропользование" на локальном ПК

## 📋 Содержание
1. [Требования к системе](#требования-к-системе)
2. [Скачивание проекта](#скачивание-проекта)
3. [Установка Python](#установка-python)
4. [Установка и настройка PostgreSQL](#установка-и-настройка-postgresql)
5. [Настройка проекта](#настройка-проекта)
6. [Запуск приложения](#запуск-приложения)
7. [Решение проблем](#решение-проблем)

---

## 🖥️ Требования к системе

- **Операционная система:** Windows 10/11, macOS 10.15+, или Linux
- **Свободное место:** минимум 500 МБ
- **RAM:** минимум 2 ГБ

---

## 📥 Скачивание проекта

### Способ 1: Скачивание с Replit (рекомендуется)

1. Откройте ваш проект на Replit
2. В левом меню нажмите на три точки **⋮**
3. Выберите **"Download as zip"**
4. Сохраните архив на компьютер
5. Распакуйте архив в удобную папку (например, `C:\Projects\mineral-licenses` или `~/Projects/mineral-licenses`)

### Способ 2: Через Git (если проект в репозитории)

```bash
git clone <URL_вашего_Git_репозитория>
cd mineral-licenses
```

---

## 🐍 Установка Python

### Windows

1. Скачайте Python 3.11 или новее: https://www.python.org/downloads/
2. Запустите установщик
3. **ВАЖНО:** Поставьте галочку **"Add Python to PATH"**
4. Нажмите **"Install Now"**
5. Проверьте установку:
   ```cmd
   python --version
   ```
   Должно вывести: `Python 3.11.x` или выше

### macOS

```bash
# Через Homebrew (если не установлен: https://brew.sh)
brew install python@3.11

# Проверка
python3 --version
```

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install python3.11 python3.11-venv python3-pip

# Проверка
python3.11 --version
```

---

## 🗄️ Установка и настройка PostgreSQL

### Windows

#### Шаг 1: Скачивание и установка

1. Перейдите на https://www.postgresql.org/download/windows/
2. Скачайте установщик для Windows (версия 14 или новее)
3. Запустите установщик `postgresql-xx-windows-x64.exe`

#### Шаг 2: Процесс установки

1. **Installation Directory:** Оставьте по умолчанию (`C:\Program Files\PostgreSQL\16`)
2. **Select Components:** Выберите все компоненты (PostgreSQL Server, pgAdmin 4, Stack Builder, Command Line Tools)
3. **Data Directory:** Оставьте по умолчанию (`C:\Program Files\PostgreSQL\16\data`)
4. **Password:** **ВАЖНО!** Придумайте и запомните пароль для пользователя `postgres` (например, `admin123`)
   - Запишите этот пароль! Он понадобится позже
5. **Port:** Оставьте `5432`
6. **Locale:** Оставьте по умолчанию или выберите `Russian, Russia`
7. Нажмите **Next** → **Install**

#### Шаг 3: Проверка установки

1. Откройте **Командную строку** (Win+R → `cmd`)
2. Выполните:
   ```cmd
   psql --version
   ```
   Должно вывести: `psql (PostgreSQL) 16.x`

#### Шаг 4: Создание базы данных

**Вариант A: Через pgAdmin (графический интерфейс)**

1. Запустите **pgAdmin 4** из меню Пуск
2. Введите мастер-пароль (который задали при установке)
3. В левой панели разверните **Servers** → **PostgreSQL 16** (введите пароль от `postgres`)
4. Правой кнопкой на **Databases** → **Create** → **Database**
5. Заполните:
   - **Database:** `mineral_licenses`
   - **Owner:** `postgres`
6. Нажмите **Save**

**Вариант B: Через командную строку**

1. Откройте **Командную строку** от имени администратора
2. Выполните:
   ```cmd
   psql -U postgres
   ```
3. Введите пароль пользователя `postgres`
4. В консоли PostgreSQL выполните:
   ```sql
   CREATE DATABASE mineral_licenses;
   \l
   ```
   (Команда `\l` покажет список баз данных, должна появиться `mineral_licenses`)
5. Выход:
   ```sql
   \q
   ```

---

### macOS

#### Шаг 1: Установка через Homebrew

```bash
# Установка PostgreSQL
brew install postgresql@16

# Запуск службы
brew services start postgresql@16

# Добавление в PATH
echo 'export PATH="/opt/homebrew/opt/postgresql@16/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

#### Шаг 2: Создание базы данных

```bash
# Создание базы данных
createdb mineral_licenses

# Проверка
psql -l | grep mineral_licenses
```

---

### Linux (Ubuntu/Debian)

#### Шаг 1: Установка

```bash
# Установка PostgreSQL
sudo apt update
sudo apt install postgresql postgresql-contrib

# Проверка статуса
sudo systemctl status postgresql
```

#### Шаг 2: Настройка пользователя и базы данных

```bash
# Переключение на пользователя postgres
sudo -u postgres psql

# В консоли PostgreSQL:
CREATE DATABASE mineral_licenses;
CREATE USER licenses_user WITH PASSWORD 'ваш_пароль';
GRANT ALL PRIVILEGES ON DATABASE mineral_licenses TO licenses_user;
\q
```

---

### Проверка подключения к PostgreSQL

**Windows:**
```cmd
psql -U postgres -d mineral_licenses
```

**macOS/Linux:**
```bash
psql mineral_licenses
```

Если вошли успешно → PostgreSQL настроен правильно! Выход: `\q`

---

## ⚙️ Настройка проекта

### Шаг 1: Переход в папку проекта

```bash
# Windows (Командная строка)
cd C:\Projects\mineral-licenses

# macOS/Linux
cd ~/Projects/mineral-licenses
```

### Шаг 2: Установка менеджера пакетов UV (рекомендуется)

UV — современный быстрый менеджер пакетов Python, который используется в проекте.

**Windows (PowerShell):**
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

**macOS/Linux:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

После установки перезапустите терминал.

**Проверка:**
```bash
uv --version
```

### Шаг 3: Создание файла переменных окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

**Windows:** создайте файл через Блокнот и сохраните как `.env` (тип: "Все файлы")

**macOS/Linux:**
```bash
nano .env
```

**Содержимое файла `.env`:**

```env
# ========================================
# НАСТРОЙКИ БАЗЫ ДАННЫХ POSTGRESQL
# ========================================

# Вариант 1: Полная строка подключения (рекомендуется)
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/mineral_licenses

# Замените:
# - postgres → ваше имя пользователя PostgreSQL
# - ваш_пароль → пароль, который вы задали при установке PostgreSQL
# - localhost → адрес сервера БД (для локального ПК всегда localhost)
# - 5432 → порт PostgreSQL (по умолчанию 5432)
# - mineral_licenses → название базы данных

# Вариант 2: Отдельные параметры (если Вариант 1 не работает)
PGDATABASE=mineral_licenses
PGUSER=postgres
PGPASSWORD=ваш_пароль
PGHOST=localhost
PGPORT=5432

# ========================================
# НАСТРОЙКИ DJANGO
# ========================================

# Секретный ключ Django (сгенерируйте свой - инструкция ниже)
SECRET_KEY=django-insecure-замените-этот-ключ-на-случайную-строку

# Режим отладки (для локальной разработки - True)
DEBUG=True

# Разрешенные хосты (для локального ПК)
ALLOWED_HOSTS=localhost,127.0.0.1

# ========================================
# ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
# ========================================

# Yandex Maps API ключ (получите на https://developer.tech.yandex.ru/)
# Без этого ключа карта будет показывать предупреждение, но сайт будет работать
# YANDEX_MAPS_API_KEY=ваш-ключ-от-яндекс-карт

# ========================================
# LDAP АУТЕНТИФИКАЦИЯ (ОПЦИОНАЛЬНО)
# ========================================
# Система поддерживает мультидоменную LDAP аутентификацию
# Пользователь может выбрать домен или система автоматически определит его
#
# Для активации: USE_LDAP=true
# Подробная инструкция: см. LDAP_MULTI_DOMAIN.md
#
# USE_LDAP=false
#
# Пример настройки для одного домена:
# LDAP_DOMAIN1_SERVER_URI=ldap://dc.company.local:389
# LDAP_DOMAIN1_BIND_DN=CN=ldap-reader,OU=Service,DC=company,DC=local
# LDAP_DOMAIN1_BIND_PASSWORD=SecurePassword123
# LDAP_DOMAIN1_USER_SEARCH_BASE=OU=Users,DC=company,DC=local
# LDAP_DOMAIN1_USER_SEARCH_FILTER=(sAMAccountName=%(user)s)
```

**Генерация SECRET_KEY:**

Откройте терминал в папке проекта и выполните:

```bash
# Через UV
uv run python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# Или через обычный Python
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

Скопируйте полученную строку и вставьте в `.env` вместо `django-insecure-замените-этот-ключ-на-случайную-строку`

### Шаг 4: Установка зависимостей Python

**Через UV (рекомендуется):**
```bash
uv sync
```

**Через pip (альтернатива):**

```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения:
# Windows:
venv\Scripts\activate

# macOS/Linux:
source venv/bin/activate

# Установка зависимостей
pip install django>=5.2.7 django-cors-headers pillow python-dotenv psycopg2-binary dj-database-url gunicorn openpyxl reportlab
```

**Примечание для Windows:** Если возникают проблемы с установкой `psycopg2-binary`, попробуйте:
```cmd
pip install psycopg2-binary --no-binary psycopg2-binary
```

### Шаг 5: Применение миграций базы данных

Миграции создадут все необходимые таблицы в PostgreSQL:

```bash
# Через UV
uv run python manage.py migrate

# Через обычный Python (если активировано виртуальное окружение)
python manage.py migrate
```

**Вы должны увидеть:**
```
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, licenses, sessions
Running migrations:
  Applying contenttypes.0001_initial... OK
  Applying auth.0001_initial... OK
  ...
  Applying licenses.0001_initial... OK
```

### Шаг 6: Создание суперпользователя

Создайте администратора для доступа к админ-панели:

```bash
# Через UV
uv run python manage.py createsuperuser

# Через обычный Python
python manage.py createsuperuser
```

**Введите:**
- **Username:** admin (или любое имя)
- **Email:** ваш@email.com (можно оставить пустым)
- **Password:** придумайте надежный пароль (символы не будут видны при вводе)
- **Password (again):** повторите пароль

**Запомните эти данные!** Они понадобятся для входа в систему.

---

## 🚀 Запуск приложения

### Запуск сервера разработки

**Через UV:**
```bash
uv run python manage.py runserver
```

**Через обычный Python:**
```bash
python manage.py runserver
```

**Успешный запуск выглядит так:**
```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).
January 07, 2025 - 15:30:45
Django version 5.2.7, using settings 'mineral_licenses.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

### Открытие сайта

Откройте браузер и перейдите по адресам:

1. **Главная страница:** http://127.0.0.1:8000/
2. **Админ-панель:** http://127.0.0.1:8000/admin/
   - Войдите с учетными данными суперпользователя
3. **Страница входа:** http://127.0.0.1:8000/login/
4. **Аналитика:** http://127.0.0.1:8000/analytics/
5. **Справка:** http://127.0.0.1:8000/help/

### Остановка сервера

Нажмите **Ctrl+C** в терминале

---

## 📊 Загрузка данных (опционально)

### Вариант 1: Загрузка через админ-панель

1. Перейдите в админ-панель: http://127.0.0.1:8000/admin/
2. Войдите с учетными данными суперпользователя
3. В разделе **Licenses** → **Licenses** → **Add License**
4. Загрузите GeoJSON файл с координатами лицензий

### Вариант 2: Загрузка тестовых данных (если есть скрипт)

```bash
# Если в проекте есть файл create_test_data.py
uv run python create_test_data.py
```

---

## ⚠️ Решение проблем

### Проблема 1: "pg_config executable not found" при установке psycopg2

**Windows:**
1. Установите Microsoft Visual C++ Build Tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. Или используйте готовый wheel:
   ```cmd
   pip install psycopg2-binary
   ```

**macOS:**
```bash
brew install postgresql
pip install psycopg2-binary
```

**Linux:**
```bash
sudo apt-get install libpq-dev python3-dev
pip install psycopg2-binary
```

### Проблема 2: "FATAL: password authentication failed for user postgres"

**Решение:**
1. Убедитесь, что пароль в `.env` совпадает с паролем PostgreSQL
2. Проверьте, что PostgreSQL запущен:
   - **Windows:** Службы → PostgreSQL → должен быть статус "Работает"
   - **macOS:** `brew services list | grep postgresql`
   - **Linux:** `sudo systemctl status postgresql`

### Проблема 3: "django.db.utils.OperationalError: could not connect to server"

**Решение:**
1. Проверьте, что PostgreSQL запущен
2. Убедитесь, что порт 5432 не занят другим приложением
3. Проверьте настройки в `.env`:
   - `PGHOST=localhost`
   - `PGPORT=5432`

### Проблема 4: "No module named django"

**Решение:**
Установите зависимости заново:
```bash
uv sync
# или
pip install -r requirements.txt
```

### Проблема 5: База данных не создалась

**Решение через pgAdmin:**
1. Откройте pgAdmin 4
2. Подключитесь к серверу PostgreSQL
3. Правой кнопкой на **Databases** → **Create** → **Database**
4. Имя: `mineral_licenses`
5. Сохраните

**Решение через командную строку:**
```bash
psql -U postgres
CREATE DATABASE mineral_licenses;
\q
```

### Проблема 6: Ошибка "Permission denied" при создании .env

**Windows:**
1. Создайте файл через Блокнот
2. **Файл** → **Сохранить как**
3. Тип файла: **Все файлы**
4. Имя файла: `.env`

### Проблема 7: Карта Yandex не отображается

**Решение:**
1. Это нормально без API ключа - будет предупреждение, но сайт работает
2. Чтобы убрать предупреждение - получите бесплатный API ключ:
   - Зарегистрируйтесь на https://developer.tech.yandex.ru/
   - Создайте JavaScript API ключ
   - Добавьте в `.env`: `YANDEX_MAPS_API_KEY=ваш-ключ`

---

## 🔒 Проверка подключения к PostgreSQL

Чтобы убедиться, что Django правильно подключается к PostgreSQL:

```bash
# Через UV
uv run python manage.py dbshell

# Через обычный Python
python manage.py dbshell
```

Если открылась консоль PostgreSQL (приглашение `mineral_licenses=#`) → всё работает!

Проверьте таблицы:
```sql
\dt
```

Должны отобразиться таблицы: `licenses_license`, `licenses_document`, и другие Django таблицы.

Выход: `\q`

---

## 📁 Структура файлов проекта

```
mineral-licenses/
│
├── mineral_licenses/          # Главное приложение Django
│   ├── settings.py           # Настройки проекта
│   ├── urls.py               # Главные URL маршруты
│   └── wsgi.py               # WSGI конфигурация
│
├── licenses/                 # Приложение лицензий
│   ├── models.py            # Модели базы данных
│   ├── views.py             # Логика представлений
│   ├── admin.py             # Настройки админ-панели
│   ├── urls.py              # URL маршруты приложения
│   └── templates/           # HTML шаблоны
│       └── licenses/
│           ├── base.html
│           ├── map.html
│           ├── login.html
│           ├── analytics.html
│           └── help.html
│
├── media/                    # Загруженные файлы (документы, GeoJSON)
├── manage.py                 # Django управляющий скрипт
├── pyproject.toml           # Зависимости проекта
├── uv.lock                  # Lockfile зависимостей UV
├── .env                     # Переменные окружения (СОЗДАЙТЕ САМИ!)
└── README.md                # Документация проекта
```

---

## ✅ Финальная проверка

После выполнения всех шагов, убедитесь:

- [ ] PostgreSQL установлен и запущен
- [ ] База данных `mineral_licenses` создана
- [ ] Файл `.env` создан с правильными настройками
- [ ] Зависимости Python установлены (`uv sync` выполнен)
- [ ] Миграции применены (`python manage.py migrate`)
- [ ] Суперпользователь создан
- [ ] Сервер запускается без ошибок
- [ ] Главная страница открывается в браузере
- [ ] Вход в админ-панель работает

---

## 🎉 Готово!

Поздравляем! Ваш сайт "База лицензий на недропользование" успешно развернут на локальном компьютере!

**Полезные команды:**

```bash
# Запуск сервера
uv run python manage.py runserver

# Создание нового суперпользователя
uv run python manage.py createsuperuser

# Применение миграций
uv run python manage.py migrate

# Создание миграций после изменения models.py
uv run python manage.py makemigrations

# Проверка проекта на ошибки
uv run python manage.py check

# Открытие консоли Django
uv run python manage.py shell

# Подключение к базе данных
uv run python manage.py dbshell
```

**Следующие шаги:**
1. Загрузите свои данные лицензий через админ-панель
2. Настройте Yandex Maps API ключ для полноценной работы карты
3. Изучите страницу справки (/help/) для понимания функционала
4. Начните работать с системой!

**Поддержка:**
При возникновении вопросов обращайтесь к разделу "Решение проблем" выше.

---

**Дата создания методички:** Январь 2025  
**Версия Django:** 5.2.7  
**Версия Python:** 3.11+  
**Версия PostgreSQL:** 14+
